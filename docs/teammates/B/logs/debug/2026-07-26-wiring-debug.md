# 2026-07-26 · Debug 日志：评测漂移 + M5/M6 回归定位

## D1 · 评测漂移（声称 96%，实测 86%）

- **现象**：接线冲刺后复跑 `run_v2_eval.py --mode direct`，EM 86% ≠ 文档声称 96%。
- **排查**：先怀疑本轮置信度接线改变了排序——排除（接线只覆盖报告值，不重排）。
  `git log -- evaluation/v2_eval_results.json` 显示结果文件停在 9c4a045（SRS 3.1），
  而 `ast_visitors.py` 在 54950ad（v3.2 对抗修复）又改过。**结果文件是老代码跑的。**
- **结论**：不是本轮引入，是 v3.2 修复轮改代码未复跑评测的存量漂移。

## D2 · M5 回归（C05/C31/C43 → None）

- **现象**：`for i in range(1,5): print(i)` + 提问"为什么只到4不到5"，不再判 M5。
- **根因**：v3.2 加了 `structural = stop_ref_in_body` 硬门控（防纯外部信号误触发），
  但这些 case 的决定性证据恰恰是提问文本，循环体内不引用 stop 值。
- **修复**：门控放宽为「结构 或 提问」，提问信号分强/弱两档（弱档需伴随边界数值），
  另加"循环变量循环后被引用"弱结构信号。对抗探针复跑 0 误报，FPR 保持 0%。

## D3 · M6 回归（C38 → None）

- **现象**：`def check_even(n): print(n % 2 == 0)` 不再判 M6。
- **根因**：v3.1 的"计算意图"判定只认 `ast.Name`（局部赋值变量）与 `ast.BinOp`，
  `ast.Compare`（比较表达式）漏了。
- **修复**：扩展 `ast.Compare` / `ast.BoolOp`。展示型函数名豁免逻辑不变，
  `show_status()` 打印比较值仍不误报。

## 当前已知限制（继承 + 更新）

1. **C13**：M5 与 M8 标签冲突（for+break 被标 M8，M8 规则面向 while）。测试标签
   问题，不动规则。当前输出 None。
2. **C41**：expected M3 有争议——`popped = nums.pop()` 是正确代码（pop 有返回值）。
   维持不判。
3. **C17**：`range(0,10,2)` + 中性提问"输出什么"，无任何误解信号。弱标签，
   维持不判——检测器不应从"提了个问题"推断"存在误解"。
4. M7 对无 stderr 的纯静态 str+int 检测仍保守（继承 v3.1 已知限制）。
5. `_fallback_m1_regex` 在含 kwargs 行 + 其他语法错误并存时理论上可误报 M1
   （对抗审查 2.8）；教学场景 M1 先验高，接受该风险。
